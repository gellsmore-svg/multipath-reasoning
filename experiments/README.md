# Experiment records

Put run records here as `*.json` matching [`record.schema.json`](record.schema.json).

This directory ships **empty of results**. Add comparisons as they are run. See [docs/experiments.md](../docs/experiments.md).

Do not commit secrets, API keys, or raw customer data.
